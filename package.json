{
    "name": "covered-call-planner",
    "version": "0.0.0",
    "description": "Deno app for planning monthly covered calls.",
    "scripts": {
        "start": "run-s cross-env",
        "cross-env": "cross-env NODE_ENV=testing sandbox"
    },
    "devDependencies": {
        "@architect/sandbox": "latest",
        "cross-env": "^7.0.3"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}
